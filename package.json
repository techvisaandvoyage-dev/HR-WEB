{
  "name": "hr-website-root",
  "version": "1.0.0",
  "private": true,
  "main": "server/server.js",
  "scripts": {
    "postinstall": "cd server && npm install",
    "build": "cd server && npm install",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {}
}
